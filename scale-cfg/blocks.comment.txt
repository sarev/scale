Function `{qualname}` does: {doc}

One-line notes on the earlier paragraphs of its body (your running context):
{priors}

Here is the NEXT paragraph of its body:

{block}

In ONE short line, say what this paragraph does - what it accomplishes, or the reason / gotcha / subtlety / edge case behind it. Always give a real description (later paragraphs rely on it); never reply 'NONE' or leave it blank.
Bare sentence. No #, no quotes, no list.
